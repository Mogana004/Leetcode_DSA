### 1) patternMatching using bulit-in method 
```java
class HelloWorld {
    public static void main(String[] args) {
        String str = "Mogana " ;
        String pattern = "oga" ;
        boolean result = str.contains(pattern) ;
        if (result){
            System.out.println("pattern forund ");
            
        }
        else{
            System.out.println("Not found ");
            
        }
     }
}
```
### 2) 
